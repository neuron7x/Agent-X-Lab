# Phase G Architecture Contract — AXL (PRODUCTION_SPEC_V2.1)

**Contract ID**: PHASE-G-AXL-2026.02  
**Scope**: UI + BFF Worker + Engine Dispatch + Evidence Chain  
**Principles**: FAIL_CLOSED • REPRODUCIBLE • EVIDENCE_FIRST • SEPARATION_OF_DUTIES

## Deliverables (must exist)

- `artifacts/AC.package` (canonical sealed package)
- `artifacts/AC_VERSION.json` (schema-valid; hash bound to AC.package)
- `artifacts/AC.signature.jws` (signature over sha256(AC.package))
- `artifacts/build.lock`, `artifacts/build.provenance`, `artifacts/artifact.sha256`, `artifacts/rebuild.log`
- `artifacts/replay.report` + `artifacts/env.fingerprint`
- `artifacts/model_check.report`, `artifacts/SMT.bridge`, `artifacts/SPS.candidates`, `artifacts/verifier.rules`, `artifacts/selection.proof`
- `ad2026_state/pb/PB-*.json` (hash-chained)
- `artifacts/SSDF.map` (coverage >= threshold; regressions=0)

## Acceptance criteria

- All PROD_SPEC gates PASS: `RRD=PRODUCTION_SPEC_V2.1`.
- No secrets in shipped bundles (scan + review).
- Deterministic selection: identical inputs -> identical chosen SPS hash.

## Separation of duties (SoD)

- Author, approver, and auditor must be distinct humans.
- Deploy requires ARB decision memo.

## Rollback conditions

- Any HARD gate FAIL.
- Any integrity anomaly.
- Any incident in SEV0/SEV1 class.
