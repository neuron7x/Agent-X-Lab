export PYTHONHASHSEED=0 LC_ALL=C LANG=C TZ=UTC GIT_PAGER=cat PAGER=cat PYTHONDONTWRITEBYTECODE=1
mkdir -p build_proof/interpretability_pr/outputs
git stash push -u -m "WIP: isolate pytest gate fix"
git status --porcelain
git rev-parse HEAD
git status --porcelain
pwd
python -V
python -c "import sys; print('\\n'.join(sys.path))"
find . -type d -name __pycache__ -prune -exec rm -rf {} + || true
find . -type f -name '*.pyc' -delete || true
find . -type d -name '.pytest_cache' -prune -exec rm -rf {} + || true
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$(pwd)" python -m pytest -q --import-mode=importlib --rootdir="$(pwd)" > build_proof/interpretability_pr/outputs/07_py_tests.txt 2>&1
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$(pwd)" python -m pytest -q --import-mode=importlib --rootdir="$(pwd)" engine udgs_core > build_proof/interpretability_pr/outputs/07_py_tests_rerun.txt 2>&1
