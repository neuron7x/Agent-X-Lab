name: pin-pip
description: Pin pip to a deterministic version and print pip --version
runs:
  using: composite
  steps:
    - shell: bash
      run: |
        : "${PIP_VERSION:?PIP_VERSION must be set}"
        python -m pip install --upgrade "pip==${PIP_VERSION}"
        python -m pip --version
