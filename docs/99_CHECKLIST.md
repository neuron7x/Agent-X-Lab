# 99 Checklist

- [ ] CHK-CI-SUPER-1: `CI Supercheck` workflow exists and runs on PR + push(main) + workflow_dispatch.
- [ ] CHK-CI-SUPER-2: `CI Supercheck` is configured as the sole required branch-protection check.
- [ ] CHK-CI-SUPER-3: Workflow hygiene includes `Action Pin Verify` API validation for pinned action SHAs.
- [ ] CHK-CI-SUPER-4: Evidence bundle exists at `build_proof/ci_supercheck_pr/` with command/output logs and EXIT_CODE lines.
