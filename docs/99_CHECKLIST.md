# 99 Checklist

- [ ] CHK-CI-REQ-1: `Python Verify` exists and is required on PR (`.github/workflows/python-verify.yml`).
- [ ] CHK-CI-REQ-2: `Action Pin Audit` exists and is required on PR (`.github/workflows/action-pin-audit.yml`).
- [ ] CHK-CI-REQ-3: CI proof captured with run URLs and failed logs if any (`build_proof/ci_calibration/ci_links.txt`, `build_proof/ci_calibration/outputs/11_gh_runs.json`, `build_proof/ci_calibration/outputs/12_gh_logs_failed.txt`).
